Currently work in progress, the idea is to have a group of html input checkboxes in divs which are dynamically changed into hidden input fields with dropdown style selectors.

If it becomes any good I may document further.

gezpage.
